# 机轮损伤可视化管理系统 — 交接文档

> 最后更新：2026-08-06
> 当前部署地址：https://6zorenjpngzt6.ok.kimi.link

---

## 一、项目概述

本项目是面向机场地勤人员的**飞机轮胎损伤可视化管理系统**，核心功能包括：

- **2D 飞机顶视图**：纯 SVG 绘制的蓝图风格飞机轮廓，轮胎以彩色圆点标注
- **轮胎状态监控**：正常（青色）、预警（黄色）、严重（红色）三级状态
- **损伤记录管理**：录入、查看、筛选航班降落后的轮胎损伤记录
- **多机型支持**：A320、B737、A330、B777、A380 共 5 种机型

---

## 二、技术栈

| 层级 | 技术 |
|------|------|
| 框架 | React 19 + TypeScript |
| 构建 | Vite 7 |
| 样式 | Tailwind CSS 3 + shadcn/ui |
| 路由 | React Router v7 (`react-router`) |
| 动画 | Framer Motion |
| 状态 | React Context (`AppContext.tsx`) |
| 图标 | Lucide React |
| 图表 | Recharts |

> 注意：项目依赖中安装了 `@react-three/fiber` 和 `three`，但**当前版本未使用 3D 渲染**，飞机视图是**纯 SVG 2D 绘制**。

---

## 三、项目文件结构

```
src/
├── main.tsx                    # 入口，挂载 App，提供 BrowserRouter
├── App.tsx                     # 路由定义：/ → TablePage, /detail/:modelId/:recordId → DetailPage
├── index.css                   # 全局样式（暗色主题基础）
│
├── types/
│   ├── aircraft.ts             # AircraftModel, TireData, TireDamage, DamageStats
│   └── record.ts               # FlightRecord, TireWound
│
├── data/
│   ├── aircraftData.ts         # 5 种机型数据 + 状态色工具函数（核心文件）
│   ├── recordData.ts           # 12 条测试记录 + 跑道/机场选项
│   └── woundMeta.ts            # 伤口类型/位置/严重度映射表（O(1) 查找）
│
├── context/
│   └── AppContext.tsx          # 全局状态：selectedModelId, selectedTireId
│
├── pages/
│   ├── Home.tsx                # 主页：左面板 + 飞机视图 + 右面板
│   ├── DetailPage.tsx          # 详情页：从 URL 解码记录数据，复用 Home 布局
│   └── TablePage.tsx           # 记录列表页：搜索、排序、录入入口
│
├── components/
│   ├── plane-view/
│   │   └── PlaneView.tsx       # SVG 飞机视图（核心视觉组件）
│   └── ui/
│       ├── LeftPanel.tsx       # 左侧面板：机型选择、统计概览、轮胎列表
│       ├── RightPanel.tsx      # 右侧面板：记录详情 / 单轮胎详情
│       ├── WoundPanel.tsx      # 伤口详情面板（RightPanel 内使用）
│       ├── DamageChart.tsx     # 轮胎历史损伤柱状图（Recharts）
│       └── AddRecordDialog.tsx # 录入新记录弹窗
│
└── components/ui/              # shadcn/ui 组件库（大量预置组件）
    ├── button.tsx, dialog.tsx, input.tsx ...
    └── （非本项目直接编写，是 shadcn 初始化生成的）
```

---

## 四、核心数据模型

### 4.1 轮胎数据 `TireData`

```typescript
interface TireData {
  id: string;           // 如 "1号"、"前-左"
  label: string;        // 如 "主起落架-左外侧"
  position: [number, number, number];  // [x, y, z] 3D 坐标（米）
  rotation?: [number, number, number];
  damageCount: number;  // 历史损伤次数
  lastInspect: string;  // 最后检查日期
  status: 'normal' | 'warning' | 'critical';
  damageHistory: TireDamage[];
  monthlyStats: number[];  // 12 个月数据
}
```

### 4.2 航班记录 `FlightRecord`

```typescript
interface FlightRecord {
  id: string;
  date: string;
  departure: string;      // 起飞地
  landingRunway: string;  // 降落跑道
  modelId: string;        // 关联机型 ID
  modelName: string;
  aircraftNo: string;     // 如 B-2378
  wounds: TireWound[];    // 本次航班的伤口列表
  status: 'normal' | 'warning' | 'critical';
}
```

### 4.3 伤口 `TireWound`

```typescript
interface TireWound {
  tireId: string;       // 关联轮胎 ID
  size: string;          // 如 "25x3mm"
  type: 'cut' | 'puncture' | 'wear' | 'bulge' | 'crack';
  position: 'tread' | 'sidewall' | 'shoulder' | 'bead';
  severity: 'low' | 'medium' | 'high';
  description: string;
}
```

---

## 五、轮胎编号规则（最关键的业务逻辑）

### 5.1 规则定义

> **面对飞机方向**（站在机头前，看向机尾）：
> - **前起落架（前轮）**：保持独立编号，如 `前-左`、`前-右`（A380 还有 `前-中`）
> - **主起落架**：从**右往左**依次编号为一号、二号、三号……

### 5.2 编号与 3D 坐标的关系

3D 坐标系定义：
- `x`：机头→机尾方向（机头为正）
- `y`：垂直地面（向上为正）
- `z`：翼展方向（飞机左侧为正，右侧为负）

**编号顺序 = z 值从大到小排列**（即从飞机左侧翼尖到右侧翼尖）

### 5.3 各机型编号对照

| 机型 | 轮胎数 | 前轮 | 主轮编号顺序 |
|------|--------|------|-------------|
| **A320** | 6 | 前-左、前-右 | 1号(左外) → 2号(左内) → 3号(右内) → 4号(右外) |
| **B737** | 6 | 前-左、前-右 | 1号(左外) → 2号(左内) → 3号(右内) → 4号(右外) |
| **A330** | 10 | 前-左、前-右 | 1号~4号(左侧翼尖→机身) → 5号~8号(右侧机身→翼尖) |
| **B777** | 14 | 前-左、前-右 | 1号~6号(左侧翼尖→机身) → 7号~12号(右侧机身→翼尖) |
| **A380** | 21 | 前-左、前-中、前-右 | 1号~5号(左翼下翼尖→机身) → 6号~9号(左机身下) → 10号~13号(右机身下) → 14号~18号(右翼下机身→翼尖) |

### 5.4 修改注意事项

若需调整编号规则，必须同步修改以下文件：
1. `src/data/aircraftData.ts` — 轮胎 `id` 和 `position` 数组顺序
2. `src/data/recordData.ts` — 所有 `tireId` 引用
3. `src/components/plane-view/PlaneView.tsx` — SVG 投影坐标确认

---

## 六、SVG 飞机视图技术细节

### 6.1 3D → 2D 投影

```typescript
function tireToSvg(cx: number, cy: number, scale: number, pos: [number, number, number]) {
  return {
    x: cx + (-pos[2]) * scale,   // z 翻转并缩放
    y: cy + (-pos[0]) * scale,   // x 翻转并缩放
  };
}
```

- 飞机 nose 在上（y 轴负方向），tail 在下
- 飞机左侧在右（z 正方向翻转）

### 6.2 飞机轮廓绘制

飞机轮廓是**纯 SVG path 手绘**，包含：
- 机身（fuselage）：带渐变的封闭 path
- 机翼（wings）：左右对称，带后掠角
- 发动机短舱（nacelles）：椭圆
- 水平尾翼（stabilizers）
- 垂直尾翼（vertical stabilizer，顶视图显示为机身尾部矩形）

所有路径坐标基于一个 800×660 的虚拟画布，通过 `scale(planeScale)` 自适应不同机型。

### 6.3 动态缩放

```typescript
const fitScale = Math.min(
  (viewW - 180) / maxSpan,   // 翼展适配
  (viewH - 100) / maxLen     // 机身长度适配
);
```

A380（21 轮）和 A320（6 轮）使用同一套 SVG 路径，通过缩放和轮胎位置差异自动适配。

---

## 七、颜色系统

### 7.1 状态色

| 状态 | 色值 | 用途 |
|------|------|------|
| 正常 | `#00D2FF` | 正常轮胎、主品牌色 |
| 预警 | `#FFD60A` | 预警轮胎、跑道号 |
| 严重 | `#FF3B30` | 严重轮胎、错误提示 |

### 7.2 背景层级

| 用途 | 色值 |
|------|------|
| 页面背景 | `#0A0A0C` |
| 面板背景 | `#0E0E10` |
| 卡片背景 | `#111114` |
| 卡片 hover | `#1A1A1E` |
| 边框 | `#1E1E22`（默认）、`#2A2A2E`（hover） |
| 次要文字 | `#5A5A60` |
| 正文文字 | `#8A8A93` |
| 主标题 | `#FFFFFF` |

---

## 八、状态管理

使用 React Context，非常轻量：

```typescript
interface AppState {
  selectedModelId: string;      // 当前选中机型
  selectedTireId: string | null; // 当前选中轮胎（null = 未选中）
  setSelectedModelId: (id: string) => void;
  setSelectedTireId: (id: string | null) => void;
  resetView: () => void;       // 取消轮胎选择
}
```

- `AppProvider` 包裹 `Home` 和 `DetailPage`
- 支持 `initialModelId` prop，用于详情页从 URL 参数初始化机型

---

## 九、路由结构

| 路径 | 页面 | 说明 |
|------|------|------|
| `/` | `TablePage` | 记录列表，搜索/排序/录入入口 |
| `/detail/:modelId/:recordId?r={base64}` | `DetailPage` | 记录详情，通过 URL query param 传递记录数据 |

### 9.1 记录数据传递方式

TablePage → DetailPage 通过 **base64 编码的 JSON** 传递：

```typescript
// 编码（TablePage）
const b64 = btoa(unescape(encodeURIComponent(JSON.stringify(record))));
navigate(`/detail/${record.modelId}/${record.id}?r=${b64}`);

// 解码（DetailPage）
const json = decodeURIComponent(escape(atob(b64)));
const record = JSON.parse(json);
```

> 必须使用 `unescape/escape` 包裹，否则中文会导致 `btoa` 报错。

---

## 十、各组件职责速查

### 10.1 PlaneView.tsx
- 纯 SVG 绘制飞机顶视图
- 轮胎点：hover 显示 tooltip，click 选中
- 选中状态：双层脉冲动画环 + 发光滤镜
- 严重/预警状态：呼吸式发光提示

### 10.2 LeftPanel.tsx
- 机型下拉选择（带动画展开）
- 损伤统计卡片（3 列数字 + 3 行状态列表）
- 轮胎列表（可点击选中，支持取消）
- 重置按钮

### 10.3 RightPanel.tsx
- 记录头部信息（航班号、状态标签、日期/起飞地/跑道）
- 内容区：
  - 未选轮胎 → 显示 `WoundPanel`（全部伤口）
  - 选中轮胎 → 显示 `DamageChart`（单轮胎历史）

### 10.4 WoundPanel.tsx
- 状态摘要 + 航班信息
- 按轮胎分组的伤口卡片
- 伤口分布横向条形图
- 维护建议

### 10.5 DamageChart.tsx
- Recharts 柱状图：展示轮胎 12 个月损伤次数
- 按伤口类型/位置/严重度分别统计

### 10.6 AddRecordDialog.tsx
- 录入新航班记录
- 基础信息（日期、起飞地、跑道、机型、飞机编号）
- 动态添加伤口（选择轮胎 → 填尺寸/类型/位置/严重度）
- 伤口数量决定记录状态（0=正常, 1-5=预警, ≥6=严重）

### 10.7 TablePage.tsx
- 记录表格：日期、起飞地、机型、飞机编号、状态、操作
- 搜索框（支持所有字段模糊匹配）
- 排序（点击表头切换 asc/desc）
- 录入按钮（打开 AddRecordDialog）

---

## 十一、数据常量速查

### 11.1 伤口类型映射（woundMeta.ts）

| key | 中文 |
|-----|------|
| cut | 划伤 |
| puncture | 扎伤 |
| wear | 磨损 |
| bulge | 鼓包 |
| crack | 裂纹 |

### 11.2 伤口位置映射

| key | 中文 |
|-----|------|
| tread | 胎冠 |
| sidewall | 胎侧 |
| shoulder | 胎肩 |
| bead | 胎圈 |

### 11.3 严重度映射

| key | 中文 | 背景色 | 文字色 |
|-----|------|--------|--------|
| low | 轻微 | `rgba(0,210,255,0.15)` | `#00D2FF` |
| medium | 中等 | `rgba(255,214,10,0.15)` | `#FFD60A` |
| high | 严重 | `rgba(255,59,48,0.15)` | `#FF3B30` |

---

## 十二、已知优化点 / 待办事项

### 12.1 已完成优化
- [x] 纯 SVG 飞机视图替代 PNG 图片
- [x] 轮胎编号规则重构（面对飞机右→左）
- [x] 性能优化：woundMeta.ts 提供 O(1) 查找映射
- [x] 性能优化：PlaneView 提取 `TireDot` memoized 子组件
- [x] 性能优化：WoundPanel 使用 `useMemo` 分组
- [x] 性能优化：AddRecordDialog 使用 `useCallback`
- [x] 修复 Unicode base64 编码中文问题

### 12.2 潜在改进方向
- [ ] **后端接入**：当前所有数据是前端 mock，需接入真实 API
- [ ] **持久化**：AddRecordDialog 新增的记录仅在内存中，刷新即丢失
- [ ] **3D 视图**：`three` 和 `@react-three/fiber` 已安装但未使用，可开发 3D 飞机模型
- [ ] **导出功能**：导出损伤报告为 PDF/Word
- [ ] **多语言**：当前仅中文，可扩展 i18n
- [ ] **权限系统**：不同角色（地勤/工程师/管理员）的视图权限
- [ ] **响应式**：当前针对桌面端优化，移动端体验一般
- [ ] **暗黑/亮色切换**：当前仅暗色主题

---

## 十三、构建与部署

### 13.1 本地开发

```bash
cd /mnt/agents/output/app
npm install
npm run dev      # 启动开发服务器
```

### 13.2 生产构建

```bash
npm run build    # 输出到 dist/ 目录
```

### 13.3 部署

```bash
# 使用 deploy_website 工具部署 dist/ 目录
# 入口文件：dist/index.html
```

---

## 十四、关键文件内容摘要

### 14.1 aircraftData.ts 核心逻辑

- `aircraftModels`：5 种机型的完整数据数组
- `generateDamageHistory()`：生成随机历史损伤数据
- `generateMonthlyStats()`：生成 12 个月统计
- `getStatusColor()` / `getStatusText()`：状态 → 颜色/文字映射

### 14.2 recordData.ts 核心逻辑

- `initialRecords`：12 条测试记录，覆盖所有 5 种机型
- `runwayOptions` / `departureOptions`：下拉选项常量

### 14.3 AppContext.tsx 核心逻辑

```typescript
const [selectedModelId, setSelectedModelId] = useState(initialModelId);
const [selectedTireId, setSelectedTireId] = useState<string | null>(null);
```

---

## 十五、联系人 / 备注

- 项目目标用户：机场地勤人员
- 核心使用场景：航班降落后，快速查看各轮胎损伤状态，决定是否需要更换
- 当前部署：https://6zorenjpngzt6.ok.kimi.link

---

*文档结束*
